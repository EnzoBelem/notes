## Como executar o celery na máquina local
### Configurando o broker
Lembrando de comentar o `CELERY_BROKER_URL` original e usar o local
No caso do broker usado na API é preciso instalar o `RabbitMQ`

Instalar o `RabbitMQ`:
``` bash
sudo apt-get update
sudo apt-get install rabbitmq-server
```

Comandos para gerenciar o  `RabbitMQ`:
``` bash
sudo systemctl start rabbitmq-server
sudo systemctl stop rabbitmq-server
sudo systemctl status rabbitmq-server
``` 

### Comandos de execução do celery
- `celery -A <app_core> beat --loglevel=info` ativa o beat do celery.
- `celery -A <app_core> worker --loglevel=info` ativa o worker do celery.
- `celery -A core worker -l info -B` ativa ambos beat e worker.

### Configurando a lista e tasks
Comentar a `CELERY_BEAT_SCHEDULE` original do projeto e criar uma apenas com as tasks que deseja testar.

